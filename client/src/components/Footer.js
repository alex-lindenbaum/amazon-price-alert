import React from 'react';

import '../styles/Footer.css';

export default () => {
    return (
        <footer>
            <p>When signing up with our service, we collect the user's email address and preferred password. This information is collected to securely identify a user with their account. All data that the user inputs is stored onto a secure MongoDB server. We do NOT distribute user data to any third parties.</p>
        </footer>
    );
};